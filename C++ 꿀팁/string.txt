
[string 문자열 K가 끝자리부터 모듈러연산을 통해 어떤수로 나누어떨어지는지 판별]

	int div = 0, temp = 1;

	for (int i = K.length() - 1; i >= 0; i--)
	{
		div = (div + (K[i] - '0') * temp) % 어떤수;
		
		temp *= 10;
		temp %= 어떤수;
	}

	if(div == 0)
		cout << "어떤수 로 나누어떨어집니다..! 나머지가 0입니다.";

==============================================================================

[개행문자가 입력될떄까지 문자열 반복문]

for(j=0;s[j]!='\0';j++)

==============================================================================

[문자열(문자) <=> 정수 형변환]

<string> 헤더파일을 추가
정수에서 스트링 변환 to_string()
스트링에서 정수로 변환 stoi()
char에서 정수로 변환 atoi()

==============================================================================

[문자열이나 벡터에서 특정원소 지우기]

string s;
s.erase(remove(s.begin(), s.end(), 지우고싶은문자), s.end());

vector<int> v;
v.erase(remove(v.begin(), v.end(), 지우고싶은원소), v.end());

==============================================================================

[소문자를 대문자로 대문자를 소문자로(char형을 매개변수)]

tolower(아스키코드), toupper(아스키코드)

==============================================================================

[문자열 거꾸로]

<algorithm> 헤더파일을 추가
a라는 문자열이 있으면,
reverse(a.begin(), a.end());


[문자열 단어순으로 정렬]

벡터에 단어들 다 집어넣고 sort(v.begin(),v.end())하면 됌

==============================================================================

[문자열 슬라이싱]

	#include <string>
	string s;
	cin >> s;

	string a = s.substr(0, 3); //0번째인덱스부터 3개

==============================================================================

[숫자를 문자열로 받았을때 나누어 떨어지는걸 판별하는 법]

	bool check(int num){
	int sum = 0;
	// 주어진 수 P 를 각 자리수별로(큰 자리수부터) 나누어 본다
	for(int i=0; P[i];i++)
		sum = (sum*10+(P[i]-'0'))%num;
	if(sum==0) return true;  //나누어떨어진다!
	return false; // 나누어 떨어지지 않는다.
}

==============================================================================

[string 헤더와 cstring(string.h) 헤더]

둘은 엄연히 다른것이다.
앞에 c가 붙거나 뒤에 h가 붙는 것은 c언어에서 있던 헤더를 가지고 오는것

string은 c++에서 새로 편하게 쓸수있도록 나온 헤더다.

memset을 쓰려면 cstring (또는 memory.h)를 써야한다.

==============================================================================

==============================================================================